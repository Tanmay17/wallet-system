const { TransactionModel } = require( '../model' );

const addTransaction = async (data, walletId) => {
    try {
    } catch (error) {
      throw error;
    }
  };
  
  module.exports = {

    addTransaction
    
  };